# Copy this file to .env and fill in values before running docker-compose

# MongoDB
MONGO_URI=mongodb://mongodb:27017
MONGO_DB=sentimentguard

# Worker security
HMAC_SECRET=change-me-to-a-strong-secret

# Ports (optional overrides)
# BACKEND_PORT=5000
# WORKER_PORT=8000
# WEB_PORT=3000
